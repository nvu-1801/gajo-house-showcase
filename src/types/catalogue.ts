export interface TocItem {
  pageIndex: number;
  title: string;
  pageLabel: string;
}

export interface DiningHighlight {
  icon: string;
  title: string;
  desc: string;
}

export interface GuideLocation {
  name: string;
  badge: string;
  desc: string;
}
